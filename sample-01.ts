console.log(' First Sample With TypeScript');

class MyClass {
    public static sum(x:number, y:number) {
        console.log('Número é: ', x + y);
        return x + y;
    }
}

MyClass.sum(5,5);
 
